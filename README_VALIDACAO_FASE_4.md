# Validação da Fase 4

Execute na raiz do repositório:

```powershell
.\VALIDAR_FASE_ATUAL.cmd
```

A fase somente está homologada quando o relatório terminar com `FASE APROVADA`.

O validador confere estrutura, versões, arquivos sensíveis, lint, TypeScript, testes, build estático, rotas exportadas, Rust, Tauri e geração do instalador desktop.
