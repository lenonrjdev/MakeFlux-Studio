# Validação da Fase 8

Execute na raiz do repositório:

```powershell
.\VALIDAR_FASE_ATUAL.cmd
```

A fase é aprovada somente após estrutura, versões, lint, TypeScript, testes, build Next.js, rotas exportadas, Rust, Tauri e instalador passarem sem falhas.

A Fase 8 adiciona testes específicos para persistência de publicações, metadados, thumbnails, calendário mensal, estados, links publicados e integração com a Produção.
