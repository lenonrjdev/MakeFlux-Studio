# Validação da Fase 20

Versão esperada: `1.7.0`.

A validação cobre rota, provedores, cofre, fallback, limites, cancelamento, schema SQLite v6, Laboratório real/demonstração, lint, TypeScript, testes, Next.js, Cargo, Tauri e instalador.

Execute `cargo fmt --manifest-path .\frontend\src-tauri\Cargo.toml` e depois `.\VALIDAR_FASE_ATUAL.cmd`.
