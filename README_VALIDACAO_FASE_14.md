# Validação da Fase 14

Execute na raiz:

```powershell
.\VALIDAR_FASE_ATUAL.cmd
```

A homologação cobre lint, TypeScript, testes acumulados, build estático, rota `/desempenho`, formatação Rust, compilação Tauri e instalador desktop.

A fase só deve ser consolidada quando o relatório terminar com `FASE APROVADA`.
