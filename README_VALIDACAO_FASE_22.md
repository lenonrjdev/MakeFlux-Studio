# Validação da Fase 22

Execute na raiz:

```powershell
cargo fmt --manifest-path .\frontend\src-tauri\Cargo.toml
.\VALIDAR_FASE_ATUAL.cmd
```

A fase está homologada somente quando o terminal imprimir `FASE APROVADA`.
