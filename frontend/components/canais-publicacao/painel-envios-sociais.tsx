import { CircleAlert, ExternalLink, History, LoaderCircle, Send } from "lucide-react";

import { provedoresCanais, rotulosStatusEnvio } from "@/data/canais-publicacao";
import type { EnvioPublicacaoSocial } from "@/types/canais-publicacao";

export function PainelEnviosSociais({ envios }: { envios: EnvioPublicacaoSocial[] }) {
  return <section className="rounded-md border border-[#e0e6e5] bg-white p-4"><div className="flex items-center gap-2 text-[10px] font-semibold text-[#303737]"><History className="size-3.5 text-[#288a75]" /> Histórico de envios</div><div className="mt-3 space-y-2">{envios.length === 0 && <div className="rounded-md border border-dashed border-[#dfe5e4] px-3 py-8 text-center text-[8px] text-[#929a99]">Nenhum envio real foi registrado.</div>}{envios.slice(0, 12).map((envio) => { const dados = provedoresCanais.find((item) => item.id === envio.provedor) ?? provedoresCanais[0]; const Icone = envio.status === "publicada" ? Send : envio.status === "falha" ? CircleAlert : LoaderCircle; return <article key={envio.id} className="flex items-start gap-3 rounded-md border border-[#e7ebea] px-3 py-2.5"><Icone className={`mt-0.5 size-3.5 shrink-0 ${envio.status === "falha" ? "text-[#b34f48]" : "text-[#278570]"}`} /><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><strong className="text-[8.5px] text-[#404847]">{dados.titulo} · {rotulosStatusEnvio[envio.status]}</strong><span className="text-[6.5px] text-[#9aa09f]">{new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short" }).format(new Date(envio.atualizadoEm))}</span></div><p className="mt-1 text-[7.5px] leading-3.5 text-[#7f8887]">{envio.mensagem}</p><div className="mt-1.5 h-1 overflow-hidden rounded-full bg-[#edf0ef]"><div className="h-full rounded-full bg-[#2d907a]" style={{ width: `${Math.max(2, envio.progresso)}%` }} /></div></div>{envio.link && <button type="button" onClick={() => window.open(envio.link ?? "", "_blank", "noopener,noreferrer")} className="foco-acessivel grid size-7 shrink-0 place-items-center rounded-md text-[#287d6b] hover:bg-[#eff7f5]"><ExternalLink className="size-3.5" /></button>}</article>; })}</div></section>;
}
