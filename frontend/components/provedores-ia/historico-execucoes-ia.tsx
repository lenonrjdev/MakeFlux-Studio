import type { RegistroExecucaoIa } from "@/types/provedores-ia";

export function HistoricoExecucoesIa({ execucoes }: { execucoes: RegistroExecucaoIa[] }) {
  return <section className="overflow-hidden rounded-md border border-[#e1e7e6] bg-white"><header className="border-b border-[#e7ebea] bg-[#fafbfb] px-4 py-3"><h2 className="text-[10px] font-semibold text-[#303637]">Execuções recentes</h2><p className="mt-1 text-[7.5px] text-[#8b9293]">Uso real, tokens, custo configurado e duração.</p></header><div className="max-h-[430px] overflow-auto">{execucoes.length === 0 ? <p className="px-4 py-8 text-center text-[8px] text-[#8b9394]">Nenhuma execução real registrada.</p> : execucoes.map((item) => <article key={item.id} className="border-b border-[#edf0ef] px-4 py-3 last:border-0"><div className="flex items-center justify-between gap-3"><strong className="text-[8.5px] font-medium text-[#41494a]">{item.provedor} · {item.modelo}</strong><span className={`rounded border px-1.5 py-0.5 text-[6.5px] ${item.status === "concluido" ? "border-[#cce3dc] bg-[#eff8f5] text-[#27705f]" : "border-[#ead5d5] bg-[#fbf4f4] text-[#965656]"}`}>{item.status}</span></div><p className="mt-1.5 line-clamp-2 text-[7px] leading-3.5 text-[#828b8c]">{item.mensagem}</p><div className="mt-2 flex flex-wrap gap-x-3 text-[6.8px] text-[#9aa1a2]"><span>{item.tokensEntrada + item.tokensSaida} tokens</span><span>{Math.round(item.duracaoMs)} ms</span><span>US$ {item.custoEstimado.toFixed(6)}</span><span>{new Date(item.criadoEm).toLocaleString("pt-BR")}</span></div></article>)}</div></section>;
}
