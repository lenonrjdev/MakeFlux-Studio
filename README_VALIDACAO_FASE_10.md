# Validação da Fase 10

Execute somente na raiz do repositório:

```powershell
.\VALIDAR_FASE_ATUAL.cmd
```

A homologação valida estrutura, versões, lint sem avisos, TypeScript estrito, testes, build estático do Next.js, rota `/configuracoes`, Cargo, Tauri e instalador desktop.

A fase está consolidada somente quando o terminal exibir `FASE APROVADA`.
